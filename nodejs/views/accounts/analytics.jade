script(type="text/javascript", src="https://www.google.com/jsapi")
script(type="text/javascript")
	var stats = !{JSON.stringify(w.stats)};
	google.load("visualization", "1", {packages:["corechart", "table"]});
	var callbacks=2;
	function waitForBoth(){
		if(--callbacks == 0) initCharts();
	}
	google.setOnLoadCallback(waitForBoth());
#chart
#table
!{assetHelpers.assetGroupTag('analytics', w.user && w.user.doc)}