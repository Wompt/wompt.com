#landing
	.channel_selector
		h1.title Wompt is Chat
		.subtitle #{w.subtitle}
		form(method='post')
			input.text.big#channel(name='channel', type='text')
			input.submit#join(type='submit', value="Join Chat Channel")
	.clear
